apiVersion: svc.dpf.nvidia.com/v1alpha1
kind: DPUService
metadata:
  name: multus
  namespace: dpf-operator-system
spec:
  helmChart:
    source:
      repoURL: ${REGISTRY}
      chart: ${MULTUS_HELM_CHART_NAME}-chart
      version: ${TAG}
