apiVersion: svc.dpf.nvidia.com/v1alpha1
kind: DPUService
metadata:
  name: multus
  namespace: dpf-operator-system
spec:
  source:
    repoURL: ${HELM_REPO}
    chart: ${MULTUS_HELM_CHART_NAME}
    version: ${TAG}