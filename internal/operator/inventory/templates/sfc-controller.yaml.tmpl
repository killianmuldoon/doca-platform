apiVersion: svc.dpf.nvidia.com/v1alpha1
kind: DPUService
metadata:
  name: sfc-controller
  namespace: dpf-operator-system
spec:
  helmChart:
    source:
      repoURL: oci://${REGISTRY}
      chart: ${SFC_CONTOLLER_HELM_CHART_NAME}-chart
      version: ${TAG}
