apiVersion: svc.dpf.nvidia.com/v1alpha1
kind: DPUService
metadata:
  name: sfc-controller
  namespace: dpf-operator-system
spec:
  source:
    repoURL: ${HELM_REPO}
    chart: ${SFC_CONTOLLER_HELM_CHART_NAME}
    version: ${TAG}