apiVersion: svc.dpf.nvidia.com/v1alpha1
kind: DPUService
metadata:
  name: ovs-cni
  namespace: dpf-operator-system
spec:
  helmChart:
    source:
      repoURL: oci://${REGISTRY}
      chart: ${OVS_CNI_HELM_CHART_NAME}-chart
      version: ${TAG}
