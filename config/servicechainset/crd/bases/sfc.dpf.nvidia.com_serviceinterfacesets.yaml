---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: serviceinterfacesets.sfc.dpf.nvidia.com
spec:
  group: sfc.dpf.nvidia.com
  names:
    kind: ServiceInterfaceSet
    listKind: ServiceInterfaceSetList
    plural: serviceinterfacesets
    singular: serviceinterfaceset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceInterfaceSet is the Schema for the serviceinterfacesets
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceInterfaceSetSpec defines the desired state of ServiceInterfaceSet
            properties:
              nodeSelector:
                description: Select the Nodes with specific labels, ServiceInterface
                  CRs will be created only for these Nodes
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              templateSpec:
                description: ServiceInterfaceSpec defines the desired state of ServiceInterface
                properties:
                  bridgeName:
                    description: The bridge name
                    type: string
                  interfaceName:
                    description: The interface name
                    type: string
                  interfaceType:
                    description: The interface type ("vlan", "physical", "pf", "vf")
                    enum:
                    - vlan
                    - physical
                    - pf
                    - vf
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to be added on created ServiceInterface CRs
                    type: object
                  node:
                    description: Node where this interface exists
                    type: string
                  pf:
                    description: The PF definition
                    properties:
                      pfId:
                        type: integer
                    required:
                    - pfId
                    type: object
                  vf:
                    description: The VF definition
                    properties:
                      parentInterfaceRef:
                        type: string
                      pfID:
                        type: integer
                      vfID:
                        type: integer
                    required:
                    - parentInterfaceRef
                    - pfID
                    - vfID
                    type: object
                  vlan:
                    description: The VLAN definition
                    properties:
                      parentInterfaceRef:
                        type: string
                      vlanID:
                        type: integer
                    required:
                    - parentInterfaceRef
                    - vlanID
                    type: object
                required:
                - bridgeName
                - interfaceName
                - interfaceType
                - node
                type: object
            type: object
          status:
            description: ServiceInterfaceSetStatus defines the observed state of ServiceInterfaceSet
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
