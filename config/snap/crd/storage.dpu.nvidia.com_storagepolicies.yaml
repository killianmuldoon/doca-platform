---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
    helm.sh/resource-policy: keep
  name: storagepolicies.storage.dpu.nvidia.com
spec:
  group: storage.dpu.nvidia.com
  names:
    kind: StoragePolicy
    listKind: StoragePolicyList
    plural: storagepolicies
    singular: storagepolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StoragePolicy represents a storage policy which maps between
          policy into a list of storage vendors.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StoragePolicySpec defines the desired state of StoragePolicy
            properties:
              storageParameters:
                additionalProperties:
                  type: string
                description: List of storage parameters supported by the policy, values
                  are string only
                type: object
              storageSelectionAlg:
                default: LocalNVolumes
                description: 'Algorithm used to select the storage vendor. Default:
                  LocalNVolumes'
                enum:
                - Random
                - LocalNVolumes
                type: string
              storageVendors:
                description: List of storage vendors
                items:
                  type: string
                type: array
            type: object
          status:
            description: StoragePolicyStatus defines the observed state of StoragePolicy
            properties:
              message:
                description: Informative message when the state is invalid
                type: string
              state:
                description: A storage policy is valid if all provided storage vendors
                  have a StorageVendor object with a valid storage class object
                enum:
                - Valid
                - Invalid
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
