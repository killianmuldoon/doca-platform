apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dpf-provisioning-volume
  namespace: dpf-operator-system
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
