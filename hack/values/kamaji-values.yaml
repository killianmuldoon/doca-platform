cfssl:
  image:
    repository: ${REGISTRY}/cfssl/cfssl
    tag: v1.6.5

image:
  # -- The container image of the Kamaji controller.
  repository: ${REGISTRY}/clastix/kamaji
  pullPolicy: Always
  # -- Overrides the image tag whose default is the chart appVersion.
  tag:

