apiVersion: "svc.dpu.nvidia.com/v1alpha1"
kind: ServiceInterfaceSet
metadata:
  name: ovn
  namespace: default
spec:
  nodeSelector:
    matchLabels:
      role: "firewall"
  template:
    metadata:
      labels:
        uplink: "port0"
    spec:
      interfaceType: ovn
