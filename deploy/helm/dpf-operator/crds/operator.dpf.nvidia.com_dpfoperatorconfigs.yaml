---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: dpfoperatorconfigs.operator.dpf.nvidia.com
spec:
  group: operator.dpf.nvidia.com
  names:
    kind: DPFOperatorConfig
    listKind: DPFOperatorConfigList
    plural: dpfoperatorconfigs
    singular: dpfoperatorconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DPFOperatorConfig is the Schema for the dpfoperatorconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DPFOperatorConfigSpec defines the desired state of DPFOperatorConfig
            properties:
              imagePullSecrets:
                description: |-
                  List of secret names which are used to pull images for DPF system components and DPUServices.
                  These secrets must be in the same namespace as the DPF Operator Config and should be created before the config is created.
                  System reconciliation will not proceed until these secrets are available.
                items:
                  type: string
                type: array
              overrides:
                description: Overrides exposes a set of fields which impact the recommended
                  behaviour of the DPF Operator
                properties:
                  disableSystemComponents:
                    description: DisableSystemComponents is a list of system components
                      that will not be deployed.
                    items:
                      type: string
                    type: array
                  paused:
                    description: Paused disables all reconciliation of the DPFOperatorConfig
                      when set to true.
                    type: boolean
                type: object
              provisioningConfiguration:
                description: ProvisioningConfiguration defines dpf-provisioning-controller
                  related configurations
                properties:
                  bfbPVCName:
                    description: BFBPersistentVolumeClaimName is the name of the PersistentVolumeClaim
                      used by dpf-provisioning-controller
                    type: string
                  dhcpServerAddress:
                    description: DHCPServerAddress is the address of the DHCP server
                      that allocates IP for DPU
                    type: string
                  dmsTimeout:
                    description: DMSTimeout is the max time in seconds within which
                      a DMS API must respond, 0 is unlimited
                    type: integer
                  imagePullSecretForDMSAndHostNetwork:
                    description: ImagePullSecretForDMSAndHostNetwork is the imagePullSecret
                      which is passed to pods created by the DPF Provisioning controller.
                    type: string
                required:
                - bfbPVCName
                - dhcpServerAddress
                - imagePullSecretForDMSAndHostNetwork
                type: object
            type: object
          status:
            description: DPFOperatorConfigStatus defines the observed state of DPFOperatorConfig
            properties:
              conditions:
                description: Conditions exposes the current state of the OperatorConfig.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration records the Generation observed on
                  the object the last time it was patched.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
